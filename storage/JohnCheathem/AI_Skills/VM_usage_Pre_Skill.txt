mmanic mode

just realized we can take advatange of AI's virtual machines

Ai studio - Limited with virtual machine internet access.

Claude - more open, can clone githubs to virtual machine

tests

Ai studio, when asked, can generated on its virtual machine a text file of 20k - 1million tokens.

i was able to download this file -  i think, may have been streaming data- and afterwards the session crashed.

either it lied, or sending the data calculates tokens, or something else. 

i know it is limited or lies about being limited and apparently cannot download anything to its VM.

but if data stored on its VM doesnt take up tokens, then we can use code to look at data, instead of having all data in token memory.

Theroty needs testing

whenn the ai send you its VM data, it can be alot.. it may work or it may crash the session.

ideas

obviously compress the data

secondly, if we could get the vm to send the data vm side, that may reduce tokens?

probably not, and gemini cant do this.

second idea, donwloading as uploading.

have 1k download options on .000001k files.
the download order writes information.
the ai download things creates a essage we can read to store data. probably not efficient.


/////////////////////////////////////////////

Claude is better

cloned whole git hub jak 1 modding db to VM and had it succesfully use code to access it efficently, unless lying very well..

it doesnt show token usage unfortnatlet so cant test that.

but still that ability is awesome!

the ai can send and recieve data from its vm, but to what extent?

can we stream images from it?

Take advatange of skills!
claude skills are powerfull.
read skill, clone repo, continue working, update skill, repeate.

each instance will clone it again

claude can download and run blender in background (no display)?!?


