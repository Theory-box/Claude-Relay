name: Build MergeAnalyzer

on:
  workflow_dispatch:
  push:
    branches: ['feature/blend-merge-analyzer']
    paths: ['apps/blend-merge-analyzer/**']
    tags: ['ma-v*']

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install build deps
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller pywebview pythonnet zstandard
      - name: Build executable
        run: pyinstaller apps/blend-merge-analyzer/merge-analyzer.spec
      - name: Package (zip the folder for a clean single download)
        run: Compress-Archive -Path dist/MergeAnalyzer/* -DestinationPath dist/MergeAnalyzer-windows.zip
      - name: Upload build
        uses: actions/upload-artifact@v4
        with:
          name: MergeAnalyzer-windows
          path: dist/MergeAnalyzer-windows.zip
      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/MergeAnalyzer-windows.zip

  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install build deps
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller pywebview pyobjc zstandard
      - name: Build executable
        run: pyinstaller apps/blend-merge-analyzer/merge-analyzer.spec
      - name: Package
        run: cd dist && zip -r MergeAnalyzer-macos.zip MergeAnalyzer
      - name: Upload build
        uses: actions/upload-artifact@v4
        with:
          name: MergeAnalyzer-macos
          path: dist/MergeAnalyzer-macos.zip
