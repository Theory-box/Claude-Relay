# Desktop Canvas

An always-on, infinite spatial canvas that replaces the Windows desktop:
PureRef-style reference board fused with file-explorer capability.
Windows-only. Built with Tauri v2 (Rust + WebView2).

Status: increment 1 — pipeline + bottom-most window proof.
See ../session-notes/desktop-canvas-progress.md for the running plan.
